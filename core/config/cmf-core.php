<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | 生产环境默认行为
    |--------------------------------------------------------------------------
    |
    | 开启后自动应用：Date 使用 CarbonImmutable、生产环境禁止破坏性数据库
    | 命令、生产环境强制强密码规则。项目不需要时设为 false。
    |
    */

    'apply_defaults' => true,

    /*
    |--------------------------------------------------------------------------
    | 停用的模块插件
    |--------------------------------------------------------------------------
    |
    | 已安装的模块默认全部挂载到后台面板。需要临时停用某个模块时，
    | 把它的 Plugin 类填到这里（类不会被实例化）。
    |
    */

    'disabled_plugins' => [
        // \Quansitech\Cmf\Users\UsersPlugin::class,
    ],

    /*
    |--------------------------------------------------------------------------
    | 面板主题
    |--------------------------------------------------------------------------
    |
    | Filament viteTheme 路径（相对项目根目录）。文件不存在时自动跳过，
    | 不影响面板使用。cmf:install 会脚手架出默认主题文件。
    |
    */

    'theme' => 'resources/css/filament/admin/theme.css',

    /*
    |--------------------------------------------------------------------------
    | 品牌设置
    |--------------------------------------------------------------------------
    |
    | name：登录页品牌区展示的名称（纯文字，不用 logo 图片），
    | 各项目按需改成自己的产品 / 系统名称。
    |
    */

    'brand' => [
        'name' => env('CMF_BRAND_NAME', '全思科技'),
    ],

];
